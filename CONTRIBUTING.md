# 贡献指南

欢迎提交 Issue 和 Pull Request。

## 开发环境

- Node.js 18 或更高版本
- 不需要安装第三方运行时依赖

## 常用命令

```bash
npm start
npm test
npm run check
```

## 提交前检查

1. 保持网易云请求为只读 `GET`，不要加入写操作接口。
2. 不要在日志、测试、截图或提交内容中包含真实 Cookie、用户 ID 或账号资料。
3. 为排序、序号解析、文件名处理和接口白名单等核心逻辑补充测试。
4. 运行 `npm run check` 并确保全部通过。

## 代码风格

- 使用 2 个空格缩进。
- 前端保持无构建步骤，直接由本地 Node.js 服务提供。
- 优先使用 Node.js 内置模块，避免引入不必要的依赖。
