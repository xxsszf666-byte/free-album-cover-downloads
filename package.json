{
  "name": "music-cover-downloader",
  "version": "1.2.0",
  "description": "A local, read-only music cover downloader for NetEase Cloud Music and QQ Music playlist share links.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js --open",
    "start:no-open": "node server.js",
    "test": "node --test",
    "check": "node --check core.js && node --check server.js && node --check public/app.js && node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "netease-cloud-music",
    "cover-downloader",
    "album-art",
    "nodejs"
  ],
  "license": "MIT"
}
